# Docker
# Build a Docker image 
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master
- azure-pipelines

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:  
  - job: Build
    displayName: Build
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - task: Docker@2
      displayName: Build and push an image
      inputs:
        command: buildAndPush
        containerRegistry: dockerHubConnection
        dockerfile: '**/Dockerfile'
        tags: |
          $(tag)
        arguments: '--build-arg ISSUER_ARG=$(ISSUER) --build-arg CLIENT_ID_ARG=$(CLIENT_ID) --build-arg REDIRECT_URI_ARF=$(REDIRECT_URI)'

